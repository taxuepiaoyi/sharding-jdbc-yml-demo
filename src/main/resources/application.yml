
server:
  port: 8092

mybatis:
  mapper-locations: classpath:mappers/*.xml
  typeAliasesPackage: com.sharding.table.entity
  configuration:
    map-underscore-to-camel-case: true

spring:
  shardingsphere:
    datasource:
      names: demo                                        # ???????????????????
      demo:
        type: com.alibaba.druid.pool.DruidDataSource            # Druid???
        url: jdbc:mysql://127.0.0.1:3306/course_db?autoReconnect=true&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull&serverTimezone=UTC                  # ??demo?url
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        password: 12345678
    sharding:
      tables:
        course:
          actual-data-nodes: demo.course_$->{1..2}        # ?????test$->{0..1}????test0?test1???
          # user$->{0..2}??user0?user1?user2???
          table-strategy:                                       # ????
            inline:
              sharding-column: cid                               # ??????????id???????"id"????
              algorithm-expression: demo.course_->{cid%2 + 1}               # ?????id?3???id%3?0????user0?
              # id%3?1????user1??id%3?2????user2?
    props:
      sql:
        show:
          true                                                  # ??sql
